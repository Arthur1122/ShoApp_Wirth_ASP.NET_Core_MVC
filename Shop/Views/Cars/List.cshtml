@model Shop.ViewModels.CarsListViewModel

<h1>All Cars</h1>
<h3>@Model.CurrCategory</h3>
<div class="row mt-5 mb-2">
    @{
        foreach (var car in Model.AllCars)
        {
            @Html.Partial("AllCars", car)
        }
    }
</div>


